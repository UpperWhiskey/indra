import { PontoTextoPipe } from './ponto-texto.pipe';

describe('PontoTextoPipe', () => {
  it('create an instance', () => {
    const pipe = new PontoTextoPipe();
    expect(pipe).toBeTruthy();
  });
});
