import { AlterarCorBotacaoDirective } from './alterar-cor-botacao.directive';

describe('AlterarCorBotacaoDirective', () => {
  it('should create an instance', () => {
    const directive = new AlterarCorBotacaoDirective();
    expect(directive).toBeTruthy();
  });
});
