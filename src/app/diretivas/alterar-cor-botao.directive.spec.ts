import { AlterarCorBotaoDirective } from './alterar-cor-botao.directive';

describe('AlterarCorBotaoDirective', () => {
  it('should create an instance', () => {
    const directive = new AlterarCorBotaoDirective();
    expect(directive).toBeTruthy();
  });
});
